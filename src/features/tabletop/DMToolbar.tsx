import { Brush, CloudFog, Eye, EyeOff, Grid3X3, ImagePlus, Layers3, Lock, MousePointer2, RotateCcw, SquareDashed, Unlock, X } from "lucide-react";
import { useRef, useState } from "react";
import type { FogTool } from "../../domain/types";
import { useTabletop } from "../../contexts/TabletopContext";

export function DMToolbar(){const {state,actions}=useTabletop();const [panel,setPanel]=useState<"fog"|"layers"|null>(null);const input=useRef<HTMLInputElement>(null);if(!state)return null;const chooseFog=(tool:FogTool)=>{actions.setFogTool(state.activeFogTool===tool?null:tool);};return <><nav className="dm-toolbar" aria-label="DM tools"><button className={!state.activeFogTool?"active":""} title="Select" onClick={()=>actions.setFogTool(null)}><MousePointer2/></button><button title="Grid"><Grid3X3/></button><button className={panel==="fog"?"active":""} title="Fog tools" onClick={()=>setPanel(panel==="fog"?null:"fog")}><CloudFog/></button><button className={panel==="layers"?"active":""} title="Scene layers" onClick={()=>setPanel(panel==="layers"?null:"layers")}><Layers3/></button><span/><button className={state.previewPlayerView?"active":""} title="Preview player view" onClick={actions.togglePlayerPreview}><Eye/></button></nav>{state.previewPlayerView&&<div className="player-preview-badge"><Eye/>PREVIEWING PLAYER VIEW<button onClick={actions.togglePlayerPreview}><X/></button></div>}{panel==="fog"&&<div className="tool-popover fog-popover"><div className="popover-title"><span><CloudFog/>Fog of war</span><button onClick={()=>setPanel(null)}><X/></button></div><div className="fog-tools"><Tool active={state.activeFogTool==="REVEAL_BRUSH"} icon={<Brush/>} label="Reveal brush" onClick={()=>chooseFog("REVEAL_BRUSH")}/><Tool active={state.activeFogTool==="REVEAL_RECT"} icon={<SquareDashed/>} label="Reveal rectangle" onClick={()=>chooseFog("REVEAL_RECT")}/><Tool active={state.activeFogTool==="HIDE_BRUSH"} icon={<Brush/>} label="Hide brush" onClick={()=>chooseFog("HIDE_BRUSH")}/><Tool active={state.activeFogTool==="HIDE_RECT"} icon={<SquareDashed/>} label="Hide rectangle" onClick={()=>chooseFog("HIDE_RECT")}/></div><p>{state.activeFogTool?"Drag on the map to paint fog.":"Choose a tool, then draw directly on the scene."}</p><div className="fog-reset"><button onClick={()=>void actions.resetFog(true)}><EyeOff/>Cover scene</button><button onClick={()=>void actions.resetFog(false)}><RotateCcw/>Clear fog</button></div></div>}{panel==="layers"&&<div className="tool-popover layers-popover"><div className="popover-title"><span><Layers3/>Scene layers</span><button onClick={()=>setPanel(null)}><X/></button></div><button className="upload-overlay" onClick={()=>input.current?.click()}><ImagePlus/><span><b>Add image overlay</b><small>PNG, JPEG, or WebP</small></span></button><input ref={input} hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>{const f=e.target.files?.[0];if(f)void actions.addOverlay(f);e.target.value=""}}/>{state.overlays.map(o=><div className="overlay-row" key={o.id}><span className={o.kind.toLowerCase()}>{o.kind[0]}</span><div><b>{o.name}</b><small>{Math.round(o.width)} × {Math.round(o.height)} · {Math.round(o.rotation*180/Math.PI)}°</small></div><button title={o.visible?"Hide":"Show"} onClick={()=>void actions.patchOverlay(o.id,{visible:!o.visible})}>{o.visible?<Eye/>:<EyeOff/>}</button><button title={o.locked?"Unlock":"Lock"} onClick={()=>void actions.patchOverlay(o.id,{locked:!o.locked})}>{o.locked?<Lock/>:<Unlock/>}</button><div className="overlay-controls"><label>Scale<input type="range" min="60" max="500" value={o.width} onChange={e=>{const width=Number(e.target.value);void actions.patchOverlay(o.id,{width,height:width*.72})}}/></label><label>Rotate<input type="range" min="-180" max="180" value={Math.round(o.rotation*180/Math.PI)} onChange={e=>void actions.patchOverlay(o.id,{rotation:Number(e.target.value)*Math.PI/180})}/></label></div></div>)}</div>}</>}
function Tool({active,icon,label,onClick}:{active:boolean;icon:React.ReactNode;label:string;onClick():void}){return <button className={active?"active":""} onClick={onClick}>{icon}<span>{label}</span></button>}
