import { ChevronDown, ChevronLeft, ChevronRight, Cloud, LogOut, MoreHorizontal, Play, Swords, Trash2, X } from "lucide-react";
import { useState } from "react";
import { Link } from "react-router-dom";
import { useTabletop } from "../../contexts/TabletopContext";

export function InitiativeBar(){const {state,actions,playerView}=useTabletop();const [expanded,setExpanded]=useState<string|null>(null);const [menu,setMenu]=useState(false);const [dragging,setDragging]=useState<string|null>(null);const [score,setScore]=useState("10");if(!state)return null;const entries=state.combat.entries;const selected=state.tokens.find(t=>t.id===state.selectedTokenId);return <header className="initiative-bar live"><Link to={`/campaign/${state.campaign.id}`} className="brand-mark"><span>W</span><div><b>WAYFINDER</b><small>{playerView?"PLAYER VIEW":state.scene.name}</small></div></Link><div className="round-control"><small>ROUND</small><strong>{String(state.combat.round).padStart(2,"0")}</strong></div>{!playerView&&<button className="turn-arrow" onClick={()=>void actions.nextTurn(-1)} aria-label="Previous turn"><ChevronLeft/></button>}<div className="turn-list">{entries.length?entries.map((entry,i)=><div className="turn-wrap" key={entry.id} draggable={!playerView} onDragStart={()=>setDragging(entry.id)} onDragOver={e=>e.preventDefault()} onDrop={()=>{if(dragging)void actions.reorderInitiative(dragging,entry.id);setDragging(null)}}><button className={`turn-card ${i===state.combat.currentIndex?"active":""}`} onClick={()=>entry.groupCount>1&&setExpanded(expanded===entry.id?null:entry.id)}><i>{entry.name[0]}</i><span>{entry.name}{entry.groupCount>1?` ×${entry.groupCount}`:""}<small>Initiative {entry.initiative}</small></span><b>{entry.initiative}</b>{entry.groupCount>1&&<ChevronDown/>}</button>{expanded===entry.id&&<div className="initiative-group"><b>Group combatants</b>{state.monsterInstances.filter(m=>m.templateId===entry.groupKey).map(m=><button key={m.id} onClick={()=>actions.selectToken(state.tokens.find(t=>t.referenceId===m.id)?.id??null)}><span>{m.customName}</span><small>{m.currentHp} / {m.maxHp} HP</small></button>)}</div>}</div>):<div className="empty-initiative"><Swords/><span><b>No active combat</b><small>{playerView?"Waiting for the DM":"Add a creature to begin"}</small></span></div>}</div>{!playerView&&<button className="turn-arrow primary" onClick={()=>void actions.nextTurn(1)} aria-label="Next turn"><ChevronRight/></button>}<div className={`sync-state ${state.connected?"":"demo"}`}><Cloud/>{state.connected?"LIVE":"DEMO"}</div>{!playerView&&<div className="combat-menu-wrap"><button className="top-more" title="Combat options" onClick={()=>setMenu(!menu)}><MoreHorizontal/></button>{menu&&<div className="combat-menu"><div><b>Combat control</b><button onClick={()=>setMenu(false)}><X/></button></div>{!state.combat.active?<button onClick={()=>void actions.startCombat()}><Play/>Start combat</button>:<button className="danger" onClick={()=>void actions.endCombat()}><X/>End combat</button>}{selected&&<form onSubmit={e=>{e.preventDefault();void actions.addToInitiative(selected.id,Number(score));}}><label>Selected creature<span>{selected.displayName}</span></label><div><input value={score} onChange={e=>setScore(e.target.value)} inputMode="numeric"/><button>Add</button><button type="button" onClick={()=>setScore(String(Math.floor(Math.random()*20)+1))}>Roll</button></div></form>}<p>Drag initiative cards to reorder.</p>{entries.map(entry=><button className="remove-entry" key={entry.id} onClick={()=>void actions.removeInitiative(entry.id)}><span>{entry.name}</span><Trash2/></button>)}</div>}</div>}{playerView&&<Link className="leave-table" to={`/campaign/${state.campaign.id}`}><LogOut/>Leave</Link>}</header>}
