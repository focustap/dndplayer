import { ChevronRight, Eye, EyeOff, Plus, RotateCcw, X } from "lucide-react";
import { useRef, useState } from "react";
import { CONDITION_OPTIONS } from "../../domain/types";
import { useTabletop } from "../../contexts/TabletopContext";

export function CreatureInspector(){const {state}=useTabletop();const token=state?.tokens.find(t=>t.id===state.selectedTokenId);if(!state)return null;if(!token)return <aside className="inspector-panel empty-inspector"><div className="empty-mark">W</div><h2>Select a creature</h2><p>Combat details appear here without covering the map.</p><small>SHIFT</small><span>Hold for map intel</span></aside>;return <InspectorContent key={token.id} tokenId={token.id}/>}
function InspectorContent({tokenId}:{tokenId:string}){const {state,actions}=useTabletop();const [amount,setAmount]=useState("8");const [editing,setEditing]=useState(false);const [conditionOpen,setConditionOpen]=useState(false);const input=useRef<HTMLInputElement>(null);const token=state?.tokens.find(t=>t.id===tokenId);const monster=state?.monsterInstances.find(m=>m.id===token?.referenceId);const character=state?.characters.find(c=>c.id===token?.referenceId);if(!state||!token)return null;const current=monster?.currentHp??character?.currentHp??0;const max=monster?.maxHp??character?.maxHp??1;const ac=monster?.ac??character?.ac??0;const speed=monster?.template?.speed??character?.speed??0;const apply=async(mode:"DAMAGE"|"HEAL")=>{const value=Number(amount);if(!monster||!Number.isFinite(value))return;await actions.adjustHp(monster.id,value,mode);setAmount("");requestAnimationFrame(()=>input.current?.focus());};return <aside className="inspector-panel"><div className="inspector-head"><div className={`portrait ${token.type==="PLAYER"?"hero":""}`}>{token.displayName[0]}</div><div><small>{token.type} · {token.visible?"VISIBLE":"HIDDEN"}</small><h2>{token.displayName}</h2><p>{monster?.template?.name??"Player character"}</p></div><button onClick={()=>void actions.patchToken(token.id,{visible:!token.visible})} aria-label="Toggle visibility">{token.visible?<Eye/>:<EyeOff/>}</button></div><div className="stat-trio"><Stat label="ARMOR" value={String(ac)}/><Stat label="SPEED" value={`${speed} ft`}/><Stat label="SIZE" value={`${token.size}×`}/></div><section className="hp-card"><div className="hp-title"><span>HIT POINTS</span><strong>{current} <i>/ {max}</i></strong></div><div className="hp-track"><i style={{width:`${Math.max(0,Math.min(100,current/max*100))}%`}}/></div>{monster&&<><label>Amount<input ref={input} value={amount} onChange={e=>setAmount(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")void apply("DAMAGE")}} inputMode="numeric" aria-label="HP amount"/></label><div className="hp-actions"><button onClick={()=>void apply("DAMAGE")}>DAMAGE</button><button onClick={()=>void apply("HEAL")}>HEAL</button></div><button className="direct-edit-toggle" onClick={()=>setEditing(!editing)}><RotateCcw/>Direct HP edit</button>{editing&&<DirectHp current={current} max={max} onSave={(c,m)=>void actions.setHp(monster.id,c,m)}/>}</>}</section><p className="section-label">CONDITIONS</p><div className="condition-list">{(monster?.conditions??character?.conditions??[]).map(c=><button key={c} onClick={()=>monster&&void actions.toggleCondition(monster.id,c)}>{c}<X/></button>)}{monster&&<button onClick={()=>setConditionOpen(!conditionOpen)}><Plus/>Add</button>}</div>{conditionOpen&&monster&&<div className="condition-picker">{CONDITION_OPTIONS.filter(c=>!monster.conditions.includes(c)).map(c=><button key={c} onClick={()=>{void actions.toggleCondition(monster.id,c);setConditionOpen(false)}}>{c}</button>)}</div>}{monster?.template&&<><p className="section-label">ABILITIES</p><div className="abilities">{Object.entries(monster.template.abilities).map(([a,v])=><span key={a}><small>{a.toUpperCase()}</small><b>{v}</b></span>)}</div><p className="section-label">TRAITS & ACTIONS</p>{[...monster.template.traits,...monster.template.actions,...monster.template.bonusActions,...monster.template.reactions].map(a=><details className="action-row" key={a.name}><summary><span><b>{a.name}</b><small>{a.attackBonus?`+${a.attackBonus} to hit · ${a.damageExpression} ${a.damageType}`:a.description}</small></span><ChevronRight/></summary><p>{a.description}</p></details>)}{monster.notes&&<><p className="section-label">DM NOTES</p><p className="dm-notes">{monster.notes}</p></>}</>}</aside>}
function Stat({label,value}:{label:string;value:string}){return <div><small>{label}</small><b>{value}</b></div>}
function DirectHp({current,max,onSave}:{current:number;max:number;onSave(c:number,m:number):void}){const [c,setC]=useState(String(current));const [m,setM]=useState(String(max));return <div className="direct-hp"><label>Current<input value={c} onChange={e=>setC(e.target.value)}/></label><label>Max<input value={m} onChange={e=>setM(e.target.value)}/></label><button onClick={()=>onSave(Number(c),Number(m))}>Save</button></div>}
