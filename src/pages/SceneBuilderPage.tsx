import { ArrowLeft, Eye, EyeOff, Grid3X3, Layers3, Sun, Upload } from "lucide-react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { TabletopProvider, useTabletop } from "../contexts/TabletopContext";
import { isDmRole, type Scene, type SceneLighting } from "../domain/types";
import { MapCanvas } from "../features/map/MapCanvas";
import { CreatureInspector } from "../features/tabletop/CreatureInspector";
import { DMToolbar } from "../features/tabletop/DMToolbar";
import { EncounterPanel } from "../features/tabletop/EncounterPanel";
import { campaignSetupService } from "../services/campaignSetupService";

export function SceneBuilderPage(){const {sceneId="",campaignId=""}=useParams();return <TabletopProvider playerView={false} sceneId={sceneId} builder><SceneBuilder campaignId={campaignId}/></TabletopProvider>}

function SceneBuilder({campaignId}:{campaignId:string}){const {state,loading,error,actions}=useTabletop();const navigate=useNavigate();if(loading)return <div className="table-loading"><span>W</span><p>Opening scene builder…</p></div>;if(error||!state)return <div className="table-loading error"><span>!</span><p>{error??"Unable to load this scene"}</p></div>;if(!isDmRole(state.role))return <div className="table-loading error"><span>!</span><p>DM access is required for Scene Builder.</p></div>;const scene=state.scene;const update=(patch:Partial<Scene>)=>void actions.updateScene(patch);const number=(key:"mapX"|"mapY"|"mapScale"|"gridSize"|"gridOffsetX"|"gridOffsetY"|"playerCameraX"|"playerCameraY"|"playerCameraZoom",value:string)=>update({[key]:Number(value)} as Partial<Scene>);const makeLive=async()=>{try{await campaignSetupService.activateScene(campaignId,scene.id);await actions.reload();}catch(e){alert(e instanceof Error?e.message:"Unable to make scene live.");}};const reveal=async(revealed:boolean)=>{try{await campaignSetupService.setSceneRevealed(scene.id,revealed);await actions.reload();}catch(e){alert(e instanceof Error?e.message:"Unable to update scene visibility.");}};return <main className="scene-builder"><header className="scene-builder-header"><Link to={`/campaign/${campaignId}`}><ArrowLeft/>Campaign setup</Link><div><small>DM-ONLY SCENE BUILDER</small><h1>{scene.name}</h1><p>{scene.active?scene.revealed?"LIVE · REVEALED TO PLAYERS":"LIVE · HIDDEN FROM PLAYERS":"DRAFT · PLAYERS CONTINUE ON THE CURRENT LIVE SCENE"}</p></div><div className="scene-builder-actions"><button className="secondary-action" onClick={()=>void makeLive()}>Make Live</button><button className="secondary-action" onClick={()=>void reveal(!scene.revealed)}>{scene.revealed?<><EyeOff/>Hide Scene</>:<><Eye/>Reveal Scene</>}</button><button className="launch-table" onClick={()=>navigate(`/campaign/${campaignId}/dm`)}><Layers3/><span><small>GAMEPLAY</small>OPEN TABLE</span></button></div></header><aside className="builder-sidebar"><section><p className="eyebrow">MAP TRANSFORM</p><NumberField label="Horizontal position" value={scene.mapX} onChange={value=>number("mapX",value)}/><NumberField label="Vertical position" value={scene.mapY} onChange={value=>number("mapY",value)}/><NumberField label="Map scale" value={scene.mapScale} min={.1} max={4} step={.05} onChange={value=>number("mapScale",value)}/></section><section><p className="eyebrow"><Grid3X3/>GRID</p><label className="builder-toggle"><input type="checkbox" checked={scene.gridType==="SQUARE"} onChange={e=>update({gridType:e.target.checked?"SQUARE":"GRIDLESS"})}/>Show square grid</label><NumberField label="Cell size" value={scene.gridSize} min={20} max={240} onChange={value=>number("gridSize",value)}/><NumberField label="Grid X offset" value={scene.gridOffsetX} onChange={value=>number("gridOffsetX",value)}/><NumberField label="Grid Y offset" value={scene.gridOffsetY} onChange={value=>number("gridOffsetY",value)}/></section><section><p className="eyebrow"><Sun/>LIGHTING</p><div className="lighting-options">{(["BRIGHT","DIM","DARK"] as SceneLighting[]).map(lighting=><button key={lighting} className={scene.lighting===lighting?"active":""} onClick={()=>update({lighting})}>{lighting}</button>)}</div></section><section><p className="eyebrow">PLAYER START CAMERA</p><NumberField label="Center X" value={scene.playerCameraX??Math.round(scene.width/2)} onChange={value=>number("playerCameraX",value)}/><NumberField label="Center Y" value={scene.playerCameraY??Math.round(scene.height/2)} onChange={value=>number("playerCameraY",value)}/><NumberField label="Zoom" value={scene.playerCameraZoom} min={.25} max={3} step={.05} onChange={value=>number("playerCameraZoom",value)}/><button className="builder-preview" onClick={actions.togglePlayerPreview}><Eye/>Player preview</button></section><section><p className="eyebrow"><Upload/>PREP</p><p className="builder-help">Use the floating tools on the canvas for initial fog, image layers, overlays, and direct placement. Draft changes stay off the player table until this scene is live and revealed.</p></section></aside><section className="builder-stage"><MapCanvas/><div className="scene-chip"><Layers3/><span><small>{scene.active?"LIVE IMAGE SCENE":"DRAFT IMAGE SCENE"}</small>{scene.name}</span></div><div className="map-hint">Space + drag to pan <span>•</span> Scroll to zoom <span>•</span> Use floating tools to place and paint</div><DMToolbar/></section><EncounterPanel/><CreatureInspector/></main>}

function NumberField({label,value,onChange,min,max,step=1}:{label:string;value:number;onChange(value:string):void;min?:number;max?:number;step?:number}){return <label className="builder-field"><span>{label}</span><input type="number" value={value} min={min} max={max} step={step} onChange={e=>onChange(e.target.value)}/></label>}
